# 智能分拣工厂：去中心化多机械臂视觉分拣系统

> 基于 Unity、计算机视觉与机器人控制构建的智能工厂原型。系统使用三台独立 SO-101 风格机械臂，对持续运动的传送带进行实时视觉感知、目标判断、抓取分拣与运行数据记录。

[观看项目演示视频](https://www.youtube.com/watch?v=sRn4y9llr-s)

## 项目概述

这个项目模拟了一条可持续运行的智能分拣产线。物体进入环形传送带后，会依次经过三个机械臂工位；每个工位拥有独立相机、检测区域、决策逻辑和机械臂控制器，可以自行完成“发现目标—判断是否来得及抓取—锁定目标—抓取—投放—回到待机”的完整流程。

系统采用去中心化设计：机械臂之间不共享目标位置，也不依赖中央任务队列。上游工位跳过或抓取失败的物体，可以继续沿传送带移动，并由下游工位通过自己的相机重新检测和处理。这种设计更接近真实产线中多个独立工位协同工作的方式，也避免了旧目标坐标在运动场景中失效的问题。

## 项目目标

- 在 Unity 中搭建一条可运行、可观察的多工位智能分拣产线。
- 将 Unity 实时画面接入外部 Python 视觉服务，完成目标检测与多目标跟踪。
- 为每台机械臂建立独立的时间窗口判断和抓取状态机。
- 验证多机械臂并行工作、目标占用冲突与下游补救机制。
- 提供可视化控制台和结构化数据记录，为后续算法分析或机器人学习准备数据。

## 核心功能

### 1. 可持续运行的虚拟产线

- 使用闭环 Spline 构建持续运动的环形传送带。
- 通过独立上料区域按时间间隔释放待分拣物体，减少重叠与遮挡。
- 在产线上布置三个互不重叠的机械臂工位及对应投放区域。
- 支持运行时启停传送带，并实时调整物体移动速度。
- 提供工厂环境照明、工位任务灯和自动巡游展示镜头。

### 2. 实时视觉检测与目标跟踪

- 三个工位分别输出 `1280 × 720` 的相机画面，并以最高 `10 FPS` 编码为 JPEG。
- Unity 通过 WebSocket 将画面、机械臂 ID、相机 ID、帧 ID 和工作区 ROI 发送到 Python 服务。
- Python 服务使用 YOLO26n 检测 `apple`、`banana`、`orange`、`bottle` 和 `cup`。
- 使用 ByteTrack 为移动目标分配稳定的 Track ID，并在短暂漏检时保留预测轨迹。
- 检测结果会映射回 Unity 相机画面，实时显示类别、置信度、边界框和跟踪状态。

### 3. 基于时间窗口的本地抓取决策

每个工位都设置了一条与真实传送带位置对应的 **Latest Pick Line**。当目标进入工作区后，系统持续计算它离开可抓取区域前的剩余时间：

```text
剩余可抓取时间 >= 抓取所需时间 + 安全余量
```

满足条件时，空闲机械臂会锁定目标并执行抓取；时间不足、置信度过低或目标已经越过最晚抓取线时，系统会跳过目标，让它继续进入下游工位。最晚抓取线只限制新任务，不会中断已经开始的抓取动作。

### 4. 多机械臂独立协同

- 三台机械臂分别处理自己的相机结果，不共享目标队列。
- 每个工位独立维护目标生命周期、决策状态和运行指标。
- 通过轻量级物体 Claim 机制，避免两台机械臂同时操作同一物理对象。
- 抓取失败或被跳过的物体不会被全局删除，下游机械臂可重新识别并再次尝试。
- 系统能够监控同时工作的机械臂数量，以及被阻止的目标占用冲突。

### 5. SO-101 风格机械臂与 IK 抓取流程

项目构建了带有六个控制通道的 SO-101 风格机械臂，包括底座、肩部、肘部、腕部和夹爪。机械臂使用 IK 驱动并通过状态机完成：

```text
解析视觉目标 → 接近 → 下探 → 闭合夹爪 → 抬升
→ 移动到投放区 → 放下物体 → 张开夹爪 → 回到初始姿态
```

系统会检测目标丢失、目标不可达、阶段超时和物体占用冲突，并在失败后释放锁定，使物体能够重新进入传送带流程。

### 6. 浏览器控制室与实时指标

FastAPI 服务同时提供一个本地浏览器控制室，用于：

- 独立启停 Session、传送带、机械臂和三路相机流。
- 查看每个工位的实时画面、视觉连接、目标、决策与动作状态。
- 查看抓取尝试、成功、失败、跳过、成功率、平均任务时间和吞吐量。
- 控制单台机械臂或相机，也可以执行全局操作。
- 安全停止 Session：停止接收新任务，并等待正在工作的机械臂回到 Idle 后再完成记录。

### 7. 面向分析与机器人学习的数据记录

每个 Session 会为三台机械臂分别生成 CSV，并额外生成元数据和本次运行的汇总文件。系统以 `10 Hz` 记录：

- SO-101 六通道关节观测与控制动作。
- 工作流状态、动作阶段和目标信息。
- 相机帧、检测类别、置信度与 Track ID。
- 目标世界坐标、剩余可抓取时间与执行/跳过决策。
- 抓取结果、失败原因、抓取耗时和完整周期耗时。

这些数据可以用于分析产线效率、定位抓取失败原因，也为后续模仿学习、策略学习或真实机械臂迁移提供数据基础。

## 系统架构

```text
Unity 工位相机
    │
    │ JPEG + 工位身份 + 帧信息 + ROI（WebSocket）
    ▼
Python / FastAPI 视觉服务
    │
    │ YOLO26n 检测 + ByteTrack 多目标跟踪
    ▼
Unity 本地目标评估
    │
    │ ROI 过滤 + Latest Pick Line + 剩余时间判断
    ▼
SO-101 IK 抓取控制器
    │
    │ 抓取、投放、失败恢复与回零
    ▼
Session CSV + 浏览器 Control Room
```

## 关键技术难点与解决方案

### 运动目标的位置具有时效性

传送带上的目标一直在移动，直接把上游工位获得的坐标传给下游工位会产生明显误差。因此系统只保留物体本身的运动连续性，不传递跨工位目标位置；每个工位必须重新检测、定位和决策。

### 检测结果会抖动或短暂消失

系统结合 ByteTrack、持续目标注册表、短时预测框和 Unity 端显示平滑，使目标 ID 和边界框在短暂漏检时仍保持连续。同时设置“连续命中后确认”和“丢失超时”规则，减少误检直接触发抓取的风险。

### 机械臂必须判断“能否及时抓到”

项目没有采用“看到就抓”的简单规则，而是把工作区投影到传送带物理路径上，根据物体速度、剩余距离、抓取耗时与安全余量动态计算最晚开始位置，从而避免对即将离开工作区的物体启动无效任务。

### 并行工位可能争抢同一物体

虽然决策逻辑保持去中心化，但物理对象仍需要最小程度的互斥保护。项目为传送带物体增加轻量 Claim，在不引入中央任务分配的前提下，避免多机械臂同时抓取同一对象。

## 技术栈

| 类型 | 技术 |
| --- | --- |
| 仿真与渲染 | Unity 6、Universal Render Pipeline、Unity Splines |
| 机器人与产线逻辑 | C#、IK、状态机、Rigidbody、Spline 运动 |
| 视觉推理 | Python、Ultralytics YOLO26n、ByteTrack |
| 服务与通信 | FastAPI、Uvicorn、WebSocket、REST API |
| 数据与监控 | CSV、浏览器 Control Room、实时 Telemetry |
| 验证 | Python `unittest`、协议/ROI/坐标映射/跟踪连续性测试 |

## 项目成果

- 完成 Unity 产线、Python 视觉推理与浏览器监控的端到端联动。
- 实现三路独立相机流和三台机械臂的并行检测、决策与抓取。
- 建立支持失败恢复与下游补救的去中心化分拣流程。
- 实现从实时帧数据到抓取 Episode 汇总的完整数据采集链路。
- 编写 13 项自动化测试，覆盖通信协议、ROI、坐标映射、检测过滤、跟踪连续性和控制状态。
- 完成可循环播放的项目展示镜头与演示视频。

## 当前状态与后续方向

当前版本已经完成可运行的端到端原型。为了让预训练视觉模型在仿真环境中稳定识别，演示对象使用带有单面类别图片的检测盒作为可控替身。

后续可以继续迭代：

- 使用写实 3D 商品模型替换检测盒，并训练面向工业物体的自定义 YOLO 权重。
- 引入更真实的夹爪接触、碰撞、质量与摩擦参数，提高抓取物理真实性。
- 将视觉模型导出为 ONNX，并评估 Unity 内推理或边缘设备部署。
- 将记录的数据接入模仿学习或强化学习流程，优化机械臂抓取策略。
- 对接真实 SO-101 机械臂，验证从数字孪生原型到实体设备的迁移路径。

